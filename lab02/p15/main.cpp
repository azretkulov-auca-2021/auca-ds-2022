#include <bits/stdc++.h>

template <typename C>
int sz(const C &c) { return static_cast<int>(c.size()); }

using namespace std;

int main()
{
    iostream::sync_with_stdio(false);

    int t;
    cin >> t;

    for (int i = 1; i <= t; i++)
    {
        vector<string> v1;
        vector<int> v2;
        int max = 0;

        for (int j = 0; j < 10; j++) {}

    }
    
}